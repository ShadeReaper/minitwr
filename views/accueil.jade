extends layoutco

block content
  link(rel='stylesheet', href='/stylesheets/accueil.css')
  br
  br
  form(action='/accueil', method='POST')
    textarea(name='tweet', rows=3, columns=50)
    input(type='submit', content='Envoyer !')
    
    - each tweet in tweets
      h5 Un connecté a posté :
      .tweet
        p= tweet
      br
